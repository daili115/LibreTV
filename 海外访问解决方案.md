# 海外访问解决方案

## 问题原因
视频源服务器（百度云、暴风资源等）有严格的地域限制，只允许中国大陆 IP 访问。
- 你的 IP: 103.151.173.197 (韩国)
- 视频源检测到海外 IP，返回 403 Forbidden: "The region has been denied"

## 解决方案

### 方案一：使用国内 VPS 部署（推荐）

1. **购买国内服务器**
   - 阿里云、腾讯云、华为云等
   - 选择北京、上海、广州等内地节点
   - 最低配置即可（1核1G 足够）

2. **部署项目**
   ```bash
   # 克隆项目
   git clone https://github.com/daili115/LibreTV.git
   cd LibreTV
   
   # 安装依赖
   npm install
   
   # 启动服务
   npm run dev
   ```

3. **配置反向代理（可选）**
   - 使用 Nginx 配置域名
   - 或直接用 IP:端口访问

### 方案二：使用国内代理服务

在 `js/config.js` 中修改代理地址：

```javascript
// 使用国内代理服务器
const PROXY_URL = 'https://api.your-chinese-proxy.com/proxy/';
```

推荐国内代理服务：
- 自建代理服务（使用国内 VPS）
- 某些付费 API 代理服务

### 方案三：使用 VPN/代理

在浏览器中启用中国大陆的代理，让请求从国内 IP 发出。

## 快速部署到国内 VPS

### 使用 Docker（推荐）
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY . .
RUN npm install --production
EXPOSE 8080
CMD ["node", "server.mjs"]
```

```bash
# 构建镜像
docker build -t libretv .

# 运行容器
docker run -d -p 8080:8080 libretv
```

### 使用 PM2 持久运行
```bash
npm install -g pm2
pm2 start server.mjs
pm2 save
pm2 startup
```

## 当前状态
- ✅ CORS 问题已修复（视频请求通过代理）
- ✅ API 搜索正常
- ❌ 视频播放失败（地域限制）

## 下一步
1. 购买国内 VPS（推荐阿里云/腾讯云，约 50-100 元/月）
2. 部署项目到国内服务器
3. 使用国内服务器地址访问
